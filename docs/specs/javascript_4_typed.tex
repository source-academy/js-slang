% Alias so this deprecated variant's PDF URL doesn't say "source" either, per the "Source" ->
% "JavaScript" rename - the real content stays in source_4_typed.tex (unrenamed body text; this
% variant is itself out of scope for the rename, only its URL is not).
\input source_4_typed.tex
