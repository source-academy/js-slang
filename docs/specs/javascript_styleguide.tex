% Alias so the style guide's PDF URL doesn't say "source" either. The real content
% (already renamed to "JavaScript Style Guide") stays in source_styleguide.tex, kept only so the
% historical source_styleguide.pdf URL keeps resolving too.
\input source_styleguide.tex
