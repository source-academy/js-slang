% Kept only so the historical source_4.pdf URL keeps resolving. The real content lives in
% javascript_4.tex, per the "Source" -> "JavaScript" language rename; use that filename in any
% new reference.
\input javascript_4.tex
